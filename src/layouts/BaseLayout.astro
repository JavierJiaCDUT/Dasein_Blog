---
import type { ImageMetadata } from 'astro';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { getSite } from '../utils/consts';

interface Props {
	title?: string;
	description?: string;
	image?: ImageMetadata | string;
	searchable?: boolean;
}

const site = await getSite();
const { title = site.author.name, description, image, searchable = true } = Astro.props;
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead title={title} description={description || site.author?.bio || ''} image={image} />
    </head>
    <body data-pagefind-ignore={searchable ? undefined : 'all'}>
        <Header />
        <main class="content-shell" data-pagefind-body={searchable ? true : undefined}>
            <slot />
        </main>
        <Footer />
    </body>
</html>
